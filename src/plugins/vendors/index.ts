/**
 * Vendor presets.
 * Import this module to register all built-in vendors.
 *
 * For custom vendors, create a separate file and register directly.
 * @see ./example.ts for template.
 */
import './example';

export {};
